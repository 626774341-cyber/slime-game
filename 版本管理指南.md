# 📦 版本管理指南（Git 三步上手）

这个项目用 Git 做版本备份。核心概念只有一句话：
**每次改完游戏，就"提交"一次 = 存一个随时可以回去的存档点。**

所有命令都在项目文件夹里执行：

```bash
cd ~/Documents/slime-game
```

## 一、日常循环：改完就存档

每次改完代码（改多少都行，小改动也建议存），执行这三行：

```bash
git add .
git commit -m "写一句这次改了什么，比如：给史莱姆加了戴墨镜功能"
git push
```

- `commit` = 在本机存一个存档点（永远可以在时光机里翻到）
- `push` = 把存档同步到 GitHub 云端（电脑坏了也不怕）

## 二、给重要版本起名字（打标签）

大改版、准备发给别人玩之前，打一个版本号标签：

```bash
git tag -a v1.1 -m "v1.1 加了墨镜功能"
git push origin v1.1
```

以后 `v1.0`、`v1.1`、`v2.0`…… 就是一批叫得上名字的存档点。

查看所有版本和历史记录：

```bash
git log --oneline --all     # 看提交历史（每行一个存档点）
git tag -l                  # 看所有版本标签
git diff v1.0 v1.1          # 对比两个版本改了什么
```

## 三、不喜欢最新版？回到之前的版本

**最常用的安全回退法（推荐，历史不会丢）：**

```bash
# 把 v1.0 的所有文件恢复出来，存成一个"回滚"新版本
git checkout v1.0 -- .
git add .
git commit -m "回滚到 v1.0"
git push
```

效果：游戏变回 v1.0 的样子，但 v1.1 的历史还在，以后想参考随时能翻出来。

**其他场景：**

```bash
# 场景 1：刚改了几行还没提交，反悔了，想丢掉这些改动
git restore .

# 场景 2：已经 commit 了但还没 push，想撤销这次提交
git reset --hard HEAD~1        # 回到上一个存档点

# 场景 3：只是想看看旧版长什么样，不改动当前版本
git checkout v1.0              # 进入"参观模式"（只读）
git checkout main              # 看完回到最新版
```

⚠️ 注意：`reset --hard` 会**丢掉**未提交的改动，执行前确认手头没有想保留的修改。

## 四、黄金习惯

1. **改一次，存一次** —— commit 的粒度 = 一个功能或一次小修，别攒一大堆再提交
2. **commit 写清楚做了什么** —— 一个月后全靠这句话找回记忆
3. **发新版前打标签** —— `v1.x` / `v2.x`，分享给别人玩的就是这个稳定版
4. **大胆改** —— 只要提交过，任何版本都回得来，Git 就是你的时光机

---
当前版本：见 `git tag -l` ｜ 线上游玩：https://626774341-cyber.github.io/slime-game/
